spring:
  application:
    name: finance-app
  datasource:
    url: jdbc:mysql://localhost:3306/finance_db
    username: root
    password: yourpassword
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080
  
